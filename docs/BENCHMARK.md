# Model Comparison Benchmark

Generated by `benchmark_runner.py --mode direct` on 2026-09-04 14:09:04.

Three algorithms evaluated across two datasets. Scikit-Learn models are scored with 5-fold stratified cross-validation; the PyTorch network is retrained across 5 random initialisations (`seeds=[42, 7, 13, 2024, 99]`) on a fixed split so that it also carries a variance estimate. All splits use `random_state=42`.

## Summary table

| Dataset | Algorithm | Framework | Validation protocol | Val accuracy (mean ± std) | Test accuracy | Time (s) |
|---|---|---|---|---|---|---|
| wine | Random Forest | scikit-learn | 5-fold stratified CV | 0.9775 ± 0.0213 | 1.0000 | 0.258 |
| wine | Kernel SVM (tuned) | scikit-learn | 5-fold stratified CV over 60 candidates | 0.9931 ± 0.0138 | 0.9444 | 45.01 |
| wine | Deep MLP (Dropout+BN) | PyTorch | repeated hold-out, 5 seeds | 0.9666 ± 0.0111 | 0.9722 | 14.969 |
| breast_cancer | Random Forest | scikit-learn | 5-fold stratified CV | 0.9543 ± 0.0150 | 0.9561 | 0.378 |
| breast_cancer | Kernel SVM (tuned) | scikit-learn | 5-fold stratified CV over 60 candidates | 0.9758 ± 0.0108 | 0.9825 | 6.445 |
| breast_cancer | Deep MLP (Dropout+BN) | PyTorch | repeated hold-out, 5 seeds | 0.9737 ± 0.0096 | 0.9825 | 8.399 |

## Per-dataset verdict

### wine

**Kernel SVM (tuned)** is nominally best, but its 0.0156 margin over Random Forest is inside the pooled standard deviation of 0.0254, so the two are statistically indistinguishable here.

Configuration of each model:

- **Random Forest** — n_estimators=50
- **Kernel SVM (tuned)** — C=10, gamma=scale, kernel=rbf
- **Deep MLP (Dropout+BN)** — hidden_dims=[64, 32], dropout=0.3, params=3267, mean gap=0.0334

### breast_cancer

**Kernel SVM (tuned)** is nominally best, but its 0.0021 margin over Deep MLP (Dropout+BN) is inside the pooled standard deviation of 0.0144, so the two are statistically indistinguishable here.

Configuration of each model:

- **Random Forest** — n_estimators=50
- **Kernel SVM (tuned)** — C=0.1, gamma=scale, kernel=linear
- **Deep MLP (Dropout+BN)** — hidden_dims=[64, 32], dropout=0.3, params=4322, mean gap=0.0219

## Reading the numbers

- Cross-validated means are the honest comparison point. A single hold-out test accuracy on datasets this small (150–569 rows) moves by several percent depending on the split, which is why every row carries a spread.
- The deep network's `generalisation_gap` (train minus test accuracy) is reported in the configuration line. Dropout and BatchNorm exist to hold that gap down; a large gap means the regularisation is too weak for the sample size.
- On tabular problems of this size, ensembles and a tuned kernel SVM are strong baselines, and a deep network has to earn its extra parameters rather than being assumed better.

## Paired cross-validation and significance tests

Every algorithm scored on the **same** 10 stratified folds (`random_state=42`). SVM hyperparameters are searched inside each outer training fold (15 candidates, 3-fold inner CV), so model selection never sees the fold it is scored on.

Differences are tested with the **Nadeau-Bengio corrected resampled t-test**, which inflates the variance estimate to account for the training-set overlap between folds. An uncorrected paired t-test on CV folds systematically overstates significance.

### wine

| Algorithm | Mean accuracy | Std | Per-fold range |
|---|---|---|---|
| Random Forest | 0.9889 | 0.0222 | 0.9444 – 1.0000 |
| Kernel SVM (tuned) | 0.9833 | 0.0255 | 0.9444 – 1.0000 |
| Deep MLP (Dropout+BN) | 0.9830 | 0.0260 | 0.9412 – 1.0000 |

| Comparison | Mean diff | t | p | Verdict (α=0.05) |
|---|---|---|---|---|
| Random Forest vs Kernel SVM (tuned) | +0.0056 | 0.383 | 0.7103 | not significant |
| Random Forest vs Deep MLP (Dropout+BN) | +0.0059 | 0.309 | 0.7644 | not significant |
| Kernel SVM (tuned) vs Deep MLP (Dropout+BN) | +0.0003 | 0.019 | 0.9853 | not significant |

### breast_cancer

| Algorithm | Mean accuracy | Std | Per-fold range |
|---|---|---|---|
| Random Forest | 0.9526 | 0.0314 | 0.8772 – 0.9825 |
| Kernel SVM (tuned) | 0.9772 | 0.0193 | 0.9474 – 1.0000 |
| Deep MLP (Dropout+BN) | 0.9701 | 0.0176 | 0.9474 – 1.0000 |

| Comparison | Mean diff | t | p | Verdict (α=0.05) |
|---|---|---|---|---|
| Random Forest vs Kernel SVM (tuned) | -0.0246 | -1.718 | 0.1199 | not significant |
| Random Forest vs Deep MLP (Dropout+BN) | -0.0175 | -0.984 | 0.3507 | not significant |
| Kernel SVM (tuned) vs Deep MLP (Dropout+BN) | +0.0070 | 0.744 | 0.4757 | not significant |
