# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts run inside WSL and MUST keep LF endings. A CRLF checkout produces
# "bad interpreter: /usr/bin/env bash^M" the moment they are executed in Linux.
*.sh text eol=lf

# PowerShell runs on the Windows side.
*.ps1 text eol=crlf
